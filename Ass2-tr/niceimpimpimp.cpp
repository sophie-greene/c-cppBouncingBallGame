#include <math.h>
#include <stdlib.h>
#include <stdio.h>
int main()
{
    float collNormalAngle;
    int n[2];
    int a1,a2;
    int optimisedP;
    collNormalAngle=ATan(btnposMatrix[][1]-j, btnposMatrix[][0]-i);
    	/* COLLISION RESPONSE
				 n = vector connecting the centers of the balls.
				 we are finding the components of the normalised vector n */
				n[0]=cos(collNormalAngle);
				n[1]=sin(collNormalAngle);
				/* now find the length of the components of each 
                movement vectors along n, by using dot product.*/
				a1 = dirx*n[0]  +  diry*n[1]

				dirx = dirx - (a1*n[0])
				diry = diry - (a1*n[1])
			
				
}
